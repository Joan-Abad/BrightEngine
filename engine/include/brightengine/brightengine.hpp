#pragma once

// Placeholder public header for the brightengine target.
//
// This exists only so that engine/include/brightengine/ is a non-empty,
// git-tracked directory and downstream targets have something to
// #include while the real engine API (RHI, ECS, renderer, ...) is
// designed. No engine logic belongs here.
