#pragma once

namespace brightengine
{
    // Only the keys actually used so far -- add more as needed.
    enum class KeyCode
    {
        W,
        A,
        S,
        D,
        Escape,
    };
}
